var Board = require("./board.js");

// demo
var board = new Board("auriga");
// board.setLedRgb();
// board.setLedRgb(r,g,b,port);
// board.setLedStripRgb(r,g,b,port,slot);
// board.setDcMotor(port, leftSpeed, rightSpeed);
// board.getSensorValue("ultrasonic", port, slot);
board.setLedRgb();